<UserControl x:Class="Photohosting.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Photohosting.Views"
             xmlns:vm="clr-namespace:Photohosting.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:RegisterViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Border MinWidth="400" Margin="15" Background="#fff" VerticalAlignment="Center" Padding="30" MaxWidth="550" Width="300" CornerRadius="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Color="LightGray" ShadowDepth="5"/>
                </Border.Effect>
                <StackPanel >
                    <TextBlock Text="Registration" FontSize="30" FontWeight="DemiBold" Margin="0 0 0 20"/>
                    <TextBox x:Name="textboxLogin" materialDesign:HintAssist.Hint="Enter login" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                    <PasswordBox x:Name="PasswordBox" materialDesign:HintAssist.Hint="Enter password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" />
                    <Button HorizontalAlignment="Center" Content="Registration" Margin="10,5,10.4,0" Style="{StaticResource MaterialDesignOutlinedButton}" Command="{Binding RegisterCommand}" CommandParameter="{Binding ElementName=PasswordBox}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
